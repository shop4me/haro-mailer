# Copy to .deploy.env (gitignored) and fill in. Do not commit .deploy.env.
#
# export DEPLOY_HOST=142.93.187.80
# export DEPLOY_USER=haro
# export DEPLOY_PATH=/home/haro/haro-mailer
#
# Prefer SSH key auth: ssh-copy-id haro@142.93.187.80
# If you must use a password (less secure), install sshpass and:
# export DEPLOY_SSH_PASSWORD='your-password'
